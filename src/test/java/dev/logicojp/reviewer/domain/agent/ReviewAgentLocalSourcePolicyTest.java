package dev.logicojp.reviewer.domain.agent;

import org.junit.jupiter.api.DisplayName;

@DisplayName("ReviewAgent local source transfer policy")
class ReviewAgentLocalSourcePolicyTest {
    // removed: local-source-per-pass transfer policy no longer exists in domain ReviewAgent; ReviewAgent is now a pure identity/config record.
}
