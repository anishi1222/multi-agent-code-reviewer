# ArchUnit configuration for this project
# failOnEmptyShould=true would cause rules to fail when no classes match the
# predicate — set to false to allow rules to pass vacuously during phases
# where the guarded packages are empty or not yet fully populated.
archRule.failOnEmptyShould=false
