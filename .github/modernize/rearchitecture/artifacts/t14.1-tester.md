# t14.1 — Remaining PM Behavior Contracts

## Summary

Fresh post-t14.2 re-pass is **PASS with 0 HIGH / 0 CRITICAL**.

- All **20 unchanged executable contracts in 8 test classes pass**.
- All 10 automatable IDs from the former 11-ID gap register now have direct green evidence.
- `AUTH-01` retains the coordinator-approved manual-tier disposition.
- Full verification passes **1,106 Surefire tests + 4 packaged-JAR Failsafe tests**, with
  0 failures, 0 errors, and 0 skipped.
- No production or test source was modified by this re-pass.

## Deliverables

- [t14.1-tester-traceability.md](./t14.1-tester-traceability.md) — final behavior-ID closure
  matrix, unchanged-test inventory, and assertion-integrity evidence.
- [t14.1-tester-runtime-validation.md](./t14.1-tester-runtime-validation.md) — environment,
  packaged-CLI startup, command results, and pass verdict.
- Eight new `src/test/java/**` classes listed in the traceability report.

## Upstream Artifacts Consumed

- `.github/modernize/rearchitecture/clarification.md` — required Java 28 and confirmed this is
  a CLI-only application with no browser/API surface; existing tests must pass.
- `.github/modernize/rearchitecture/team/tester/inbox.md` §`t14.1 — queued` — established the
  TGT-07 and multilingual priorities and accepted `AUTH-01` as manual-tier.
- `.github/modernize/rearchitecture/artifacts/t3-pm.md` §§5.2–5.8 — authoritative behavior
  contracts and acceptance outcomes.
- `.github/modernize/rearchitecture/artifacts/t14.1-tester.md` (pre-remediation revision) and
  its linked detail files — supplied the 11-ID register, five exact failing assertions, and
  1,106-test baseline.
- `.github/modernize/rearchitecture/artifacts/t14.2-backend.md` — identified the remediated
  `SKL-07`, `RTY-04`, and `AUTH-10` paths and required the original contract tests to remain
  unchanged.

## Evidence Mapping

| Upstream contract / section | This task output or evidence |
|---|---|
| `t3-pm.md` §5.2 `SKL-05/06` | `SkillParameterSafetyContractTest` — boundary +1 rejection, at-boundary control, homoglyph injection rejection, ordinary-value control |
| `t3-pm.md` §5.2 `SKL-07`; §5.7 `RTY-04` | `SkillExecutionResilienceContractTest` — exact 2/2/8 attempt counts, permanent-failure single-attempt control, circuit-open behavior, timeout and exact empty-result mappings all pass |
| `t3-pm.md` §5.3 `INS-01/02/03` | `InstructionNormalizationDefenseTest` — NFKC, U+200B stripping, Cyrillic, Korean, and Simplified Chinese, each paired with a safe negative control |
| `t3-pm.md` §5.4 `TGT-07` | `LocalReviewSymlinkDefenseTest` — real external symlink excluded while a safe sibling file is collected |
| `t3-pm.md` §5.5 `ORC-05` | `AgentReviewSemaphoreBehaviorTest` — second acquire is observed before asserting queueing; maximum active execution remains one |
| `t3-pm.md` §5.6 `AUTH-01/10` | `AUTH-01` retained as approved manual-tier; `CopilotDeprecatedTokenWarningContractTest` captures a pre-initialization WARN containing `deprecated` and excluding the token |
| `t3-pm.md` §5.8 `OUT-03/09` | `ReportFilenameBehaviorContractTest` and `ReviewDualOutputBehaviorTest` — numbered multi-pass files, single-pass control, stdout completion, and a persisted report |
| `inbox.md` lines 229–231 | Security-first test ordering, KO/ZH + Cyrillic coverage, and manual-tier `AUTH-01` disposition in the traceability report |
| `t14.1-tester.md` pre-remediation §§Findings/Test Results → `t14.2-backend.md` §§Implementation/Test Results | The same 20 current contracts pass; the five formerly red methods are green without relaxed assertions |
| `clarification.md` lines 34–50 | Java 28 activation, packaged CLI startup, full single-module Maven verification, browser E2E marked N/A by design |

## Test Results

- Packaged CLI build:
  `JAVA_HOME=~/.sdkman/candidates/java/28.ea.9-open ./mvnw -B -q clean package -DskipTests`
  - Exit code: **0**
- Startup:
  `JAVA_HOME=~/.sdkman/candidates/java/28.ea.9-open ~/.sdkman/candidates/java/28.ea.9-open/bin/java -jar target/multi-agent-reviewer-1.0.0-SNAPSHOT.jar --help`
  - Exit code: **0**
  - Result: Micronaut `[cli]` environment established; usage printed; Copilot lifecycle
    started and stopped cleanly.
- All remaining behavior contracts:
  `JAVA_HOME=~/.sdkman/candidates/java/28.ea.9-open ./mvnw -B -Dtest=SkillParameterSafetyContractTest,InstructionNormalizationDefenseTest,LocalReviewSymlinkDefenseTest,AgentReviewSemaphoreBehaviorTest,ReportFilenameBehaviorContractTest,ReviewDualOutputBehaviorTest,SkillExecutionResilienceContractTest,CopilotDeprecatedTokenWarningContractTest test`
  - Exit code: **0**
  - Passed: **20**
  - Failed: **0**
  - Errors: **0**
  - Skipped: **0**
- Full command:
  `JAVA_HOME=~/.sdkman/candidates/java/28.ea.9-open ./mvnw -B clean verify`
  - Exit code: **0**
  - Surefire: **1,106 passed / 0 failed / 0 errors / 0 skipped**
  - Failsafe packaged-JAR smoke: **4 passed / 0 failed / 0 errors / 0 skipped**
  - Architecture: Rule 0 parsed **365/365**; Rule 1 and Rule 5 each inspected their
    full subjects with **0 violators**.

Count reconciliation remains **1,086 pre-t14.1 baseline + 20 contracts = 1,106**.
Surefire XML contains 1,106 `<testcase>` elements; its root `tests` attributes under-count
parameterized expansion by the known offset of 9 and are not used as the verdict count.

Assertion-integrity check: the 8 files still declare **15 plain tests + 5 parameterized
cases = 20**, exact attempt/message/redaction and matched-control assertions remain present,
and every file's SHA-256 was identical before and after this re-pass.

## Findings

None — **0 HIGH / 0 CRITICAL**. The three former production gaps are closed by direct,
unchanged-contract evidence. `AUTH-01` is explicitly manual-tier, not silently unverified.

## Status

**PASS** — all automatable former gaps are green, the accepted manual-tier item is explicit,
and the full clean verification exits 0. No production or test source was changed by this re-pass.
